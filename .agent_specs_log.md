# Agentic Memory and Documentation Log

Questo documento è un registro tecnico strutturato a uso esclusivo delle AI agentiche che lavorano su questo workspace. Contiene dati tecnici strutturati, vincoli, modifiche e cronologia delle decisioni ingegneristiche.

| Data e Ora | Componente Target | Specifica Precedente / Stato Iniziale | Nuova Specifica / Modifica Applicata | Vincolo Logico Derivato |
| :--- | :--- | :--- | :--- | :--- |
| 2026-06-09 11:57 | Report Tolleranze (EN 13231-3) | Stato volatile o localStorage generico | Salvataggio permanente obbligatorio dei valori di tolleranza inseriti | Garantire la persistenza dei valori all'inserimento e al ricaricamento (localStorage / file di configurazione) |
| 2026-06-10 12:01 | Tabella dei Difetti (DataVizualizer.jsx) | Sezione "Report Difetti" mostrava solo card aggregate (count totale fuori-soglia per canale) | Implementato componente `DefectTable.jsx` + algoritmo segmentazione difetti: righe CSV consecutive dove `|val| > threshold` → una sezione con `kmStart`, `kmEnd`, `maxDefect`; Priority = `(maxDefect - threshold)/threshold*100`. Export CSV/Excel/PDF (HTML print). Checkbox PDF e Validated per riga. Sort, filter, grouping per canale. | Tolleranze persistono via API `saveTolerances`; flags validated/pdf sono in-memory (futuro: endpoint `saveDefects` analogia `saveSingularities`). Build `npm run build` verificato OK. |
| 2026-06-10 15:10 | Algoritmo Segmentazione Difetti (DataVizualizer.jsx) | Priority B mancante; ID riga basato su intero incrementale volatile (reset ad ogni render → flag validated/pdf persi) | (1) Aggiunta **Priority B** = punti fuori soglia / punti totali sezione × 100. (2) **ID stabile** tipo stringa composita `{col}::{kmStart}::{kmEnd}` — i flag validated/pdf sopravvivono ai re-render. (3) Reset corretto di `segTotalPts`/`segOutPts` ad apertura e chiusura di ogni segmento. | `defectFlags` indicizzato con l'ID stabile; l'export CSV/Excel/PDF eredita `priorityB` già presente in `DefectTable.jsx`. ESLint zero errori. |
| 2026-06-10 21:43 | DataVizualizer.jsx (Rendering, Stato) | Acquisizione file singola (`csvData`), no supporto per offset né .geo multipli contemporanei. | Introduzione array `geoDatasets` per gestire `.geo` multipli. Render congiunto `csvData` e `geoDatasets` con offset traslato sull'asse X. UI Modale per gestione file e bottone di Auto-Allineamento (algoritmo least-squares su finestra di $\pm100$m). | L'offset modale e il refactoring grafico escludono i dati raw `csvData` dal shift; l'allineamento è limitato al primo canale Y selezionato. |
| 2026-06-10 22:25 | PROMPT DIFETTI PER SEZIONE.md | Richiesta generica di visualizzazione difetti in sezioni da 200m con card rettangolare. | Prompt strutturato ed esteso con sezioni normative (EN 13231-3), logica di calcolo gravità e requisiti di interfaccia UI/UX (heatmap/bar-chart orizzontale, scala cromatica, tooltips). | I requisiti UI suggeriscono l'implementazione di un componente grafico addizionale (es. heatmap o barra segmentata interattiva) da posizionare sotto il grafico principale. |
| 2026-06-10 23:12 | DataVizualizer.jsx (Heatmap Difetti per Sezione) | I blocchi della heatmap non presentavano un asse X e separatori netti. Il Km nel tooltip non era formattato. | 1) Aggiunto uso `formatRailwayKm(km*1000)` nei tooltip. 2) Aggiunte linee verticali tratteggiate `border-dashed` sui segmenti. 3) Aggiunta asse X sottostante con etichette chilometriche. Aggiornato documento PROMPT relativo. | L'uso di `flex-1` per la scala X e per i blocchi assume che le sezioni contigue abbiano la stessa larghezza. Se mancano dati (gap), i blocchi adiacenti si uniformano perdendo linearità. |
| 2026-06-10 23:15 | DataVizualizer.jsx (Bugfix Import Order) | Vite crashava con SyntaxError per function declaration posta prima degli statement `import`. | Spostata `formatRailwayKm` dopo i blocchi di `import` per ripristinare la renderizzazione di React. | Le function helper locali devono essere dichiarate strettamente dopo i blocchi di import nei moduli ES. |
| 2026-06-15 — | Architettura Multi-Tenant Cliente/Progetto/Sistema/Moduli | Schema attuale: `Client → Product → File`. Progetti/Sistemi/Moduli solo documentati, non in DB. | Gerarchia obbligatoria: **Cliente** → **N Progetti** (quota solo `adts-superuser`) → **X Sistemi/progetto** → **moduli abilitati per sistema**. Documenti: `proposta di architecttura.md`, `PROMPT proposta di architettura.md`. | Provisioning riservato a superadmin. Implementazione in 6 fasi (DB → backend provisioning → middleware → UI admin → UI cliente → cleanup Product). |
| 2026-06-18 14:05 | Logica UI / Sidebar & Routing WebOne | Assenza del modulo RailProfile nella UI di navigazione. | Aggiunta voce "RailProfile" (icona cartella) nella Sidebar. Creata RailProfilePage come schermata vuota e aggiunto il routing in App.jsx per /railprofile. | La route /railprofile agisce come placeholder protetto in vista dell'integrazione a micro-frontend. |
| 2026-06-23 10:20 | TaipeiScaffold (frontend/app.js, server.py, start_scafold.bat) | Le coordinate trascinate venivano salvate solo in localStorage, andando perse al riavvio del PC o se si apriva il bundle standalone html. | Creato server.py con endpoint POST /api/save-station che aggiorna stabilmente data/stations.json e frontend/app.js e rigenera l'HTML bundle. Nel drag end del frontend richiamata la fetch di salvataggio. | Le coordinate aggiornate persistono su disco e nel codice sorgente, rimanendo stabili a ogni build o riavvio del sistema. |
| 2026-06-26 17:04 | Specifiche/software-specification.md | File vuoto ("d"). | Estrapolate le specifiche da `architecture_overview.pdf` e redatte in Markdown strutturato con descrizione servizi, stack, rotte ed esecuzione richieste. | Nessun vincolo software diretto derivato (documento di specifica). |
| 2026-06-26 17:08 | Specifiche/struttura_directory.md | File vuoto. | Redatto documento con la descrizione della struttura delle directory generali e i dettagli della gerarchia fisica e logica dei moduli (filesystem e codice). | Nessun vincolo software diretto derivato (documento di specifica). |
| 2026-07-05 08:21 | Architettura Workspace (track_web-main) | track_web-main in E:\Software\track_web-main | Spostamento in E:\Software\RailPulse\track_web-main. Aggiornati percorsi assoluti in config.json e pulizia .next. | Richiesta rigenerazione cache Next (npm run build/dev) per ripristino percorsi locali. |
| 2026-07-05 08:36 | Documentazione (rsp_specifiche.md) | File presente in E:\Software\rspy | Spostamento del file intro progetto in E:\Software\RailPulse\Specifiche\rsp_specifiche.md. | Il documento di specifiche principali per Raspberry/TGM Visualiser fa ora parte dell'albero di RailPulse. |
| 2026-07-05 08:48 | maintenance-web (Modulo Manutenzione) | Modulo non esistente, cartella vuota. | Creato modulo Express backend + React/Vite frontend in `maintenance-web`. Database JSON in `database/maintenance_db.json`. Specifiche in `Specifiche/mnt_spec.md`. | Integrazione futura basata su query parameter e overlay spaziali km per `track-view` (TGM) e link con `rpId` per RailProfile. |
| 2026-07-05 08:56 | Specifiche/gen_layout.md (Design System) | Uso di icone generico consentito nei bottoni/logo. | Aggiunto divieto tassativo dell'uso di icone grafiche (SVG, emoji, font, Lucide) nei pulsanti, header, tabelle e menu. Interfaccia puramente testuale. | Gli elementi dell'interfaccia utente devono essere puramente tipografici; le icone devono essere rimosse o sostituite da testo. |
| 2026-07-05 08:58 | maintenance-web/frontend (UI Refactoring) | Interfaccia in Dark Mode e con icone/emoji. | Riprogettata la UI in Light Mode conformemente a `gen_layout.md`, rimuovendo tutte le icone ed emoji nei bottoni, intestazioni e grafici. | I file compilati sono serviti staticamente dal server Express dalla cartella `dist`. |
| 2026-07-05 09:19 | general-configuration_web (Modulo Configurazione Comune) | Modulo non esistente, cartella vuota. | Creato modulo Express backend + React/Vite frontend in `general-configuration_web`. Database JSON in `database/config_db.json`. Specifiche in `Specifiche/gc_spec.md`. Gestisce lingua, linee/binari, operatori e tipologie interventi con colori e icone. | Gli altri moduli (come `maintenance-web`) in futuro consumeranno queste configurazioni comuni tramite le API esposte (porta 5002) anziché averle hardcoded. Il modulo rispetta la regola "Esclusione Icone" nella UI. |
| 2026-07-05 09:38 | Database TGM (track_web-main) | Database localizzato in `track_web-main/database`. | Spostamento di tutti i dati del database in `E:\Software\RailPulse\DATABASE\TGM`. Aggiornato `config.json` (operatore Superman) e tutte le specifiche tecniche correlate (`tgm_specifiche_database.md`, `tgm_import_file.md`). | Le sessioni TGM ora risiedono nella cartella globale `DATABASE/TGM`, e il modulo le legge/scrive direttamente da lì. |
| 2026-07-05 09:45 | Internazionalizzazione e Integrazione Modulo | Lingua hardcoded in italiano in `maintenance-web`. Modulo `general-configuration_web` non accessibile da `WebOne/files`. | (1) `maintenance-web` recupera la lingua attiva da `general-configuration_web` (porta 5002) e traduce la UI (`it`/`en`/`zh`). (2) Aggiunto il modulo `general-configuration_web` alla pagina dei progetti in `WebOne` (porta 5002). | La localizzazione mantiene i valori enum originali in DB per la consistenza dei dati diagnostici. |
| 2026-07-05 09:52 | Migrazione station.json nel Database Generale | `station.json` collocato in `DATABASE/TGM/station.json` e letto localmente in `track_web-main`. | Spostato `station.json` in `E:\Software\RailPulse\DATABASE\station.json`. Aggiornato `stationManager.js` di `track_web-main` per calcolare la root `DATABASE` e leggere/scrivere la lista stazioni a livello generale. | Garantisce che la lista stazioni sia condivisa globalmente e accessibile a tutti i sotto-moduli diagnostici (TGM, etc.). |
| 2026-07-05 17:00 | track-view (track_web-main) | Next.js 15.1.0 app fullstack monolitica | Migrazione architetturale in due progetti separati: `frontend` (Vite/React, porta 3000) e `backend` (Express, porta 5003). Riscrittura delle API e del routing. Aggiornato `start.bat`. Rimozione dei file Next.js obsoleti. | Il backend Express forza `process.cwd()` sulla directory root del modulo (`track_web-main`) tramite `process.chdir()` all'avvio per mantenere intatta la compatibilità con i percorsi relativi esistenti (`DATABASE`, `configuration`). |
| 2026-07-05 17:05 | ConfigurationPage.jsx (track_web-main) | Sezione parametri bloccata via pointer-events-none e bottone salvataggio disabilitato se no operator | Se non ci sono operatori registrati, viene forzato l'inserimento di un nuovo operatore (`isCreatingNew = true`). Il pulsante di salvataggio è abilitato per consentire il salvataggio dei parametri globali email anche senza operatore. | Garantisce la configurabilità della sezione email e un'interazione fluida a database vuoto. |
| 2026-07-05 17:15 | File Browser & TGM Fallback (track_web-main) | Navigazione iniziale file browser dai dischi locali e fallback 'database' | Il File Browser si apre per impostazione predefinita nella cartella `DATABASE/TGM` se nessun percorso è impostato (risolto dal backend rispetto alla radice del workspace `..`). Aggiornati i fallback a `DATABASE/TGM` nei componenti frontend per allineare l'applicazione con il DB globale. | Consente di accedere e selezionare immediatamente la cartella delle sessioni TGM senza dover navigare a partire dalle unità disco. |
| 2026-07-05 21:50 | DATABASE/station.json | Array di stringhe semplici (solo codice). | Array di oggetti JSON in lingua inglese con campi: `code`, `name`, `kmStart`, `kmEnd`, `tracks`. Backend `stationManager.js` garantisce migrazione e parsing retrocompatibile dei campi in italiano e stringhe semplici. La pagina Configuration TGM supporta operazioni CRUD complete. | Tutte le entità `station.json` devono avere struttura a oggetti in inglese. Il frontend e il backend TGM devono consumare questa struttura per l'editing e la validazione. |
| 2026-07-05 21:35 | Modulo Manutenzione (Edit e Allegati) | Modalità modifica raggiungibile solo dal dettaglio; nessun supporto file. | Aggiunto pulsante "Edit" (Azioni) in tabella. Aggiunta dropzone file Drag & Drop nel modale di modifica. Endpoint `/upload` (Multer) configurato in `maintenance-web` e WebOne (`app.js`) per salvare in `uploads/maintenance` servita staticamente. | Gli allegati di manutenzione sono caricati dinamicamente, salvati localmente in `uploads/maintenance` e i path relativi sono referenziati in `attachments` come stringhe. |
| 2026-07-06 15:10 | Integrazione TaipeiScaffold (WebOne) | Standalone app. | Convertito in modulo Iframe integrato in WebOne. Frontend servito come statico (`/taipei_static`). API `/api/taipei/save-station` creata in Node.js per rigenerazione via Python. Dati spostati in `DATABASE/Taipei`. Pagina React `TaipeiScaffoldPage.jsx` creata. | L'architettura prevede l'uso dell'Iframe per contenere il Vanilla JS legacy in isolamento e la posizione dei dati in `DATABASE/Taipei`. |
| 2026-07-06 15:30 | Schema `DATABASE/station.json` | Array di oggetti semplici (code, name, kmStart, kmEnd, tracks). | Esteso lo schema per includere `lineCode` (string), `stationNumber` (string/number), `x` (number), `y` (number), `stationType` (enum: "station", "main_station"). | La struttura dei nodi stazione deve sempre includere coordinate e metadati estesi per futuri utilizzi in grafi o mappe interattive. |
