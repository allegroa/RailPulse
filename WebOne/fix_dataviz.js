const fs = require('fs'); let code = fs.readFileSync('frontend_webbone/src/pages/DataVizualizer.jsx', 'utf-8'); if (!code.includes('useTranslation')) { code = code.replace('import { useLocation } from \\'react-router-dom\\';', 'import { useLocation } from \\'react-router-dom\\';\\nimport { useTranslation } from \\'react-i18next\\';'); } if (!code.includes('const { t, i18n } = useTranslation();')) { code = code.replace('export default function DataVizualizer() {', 'export default function DataVizualizer() {\\n  const { t, i18n } = useTranslation();\\n  const [hoveredCoords, setHoveredCoords] = useState(null);'); } const oldOptions = '  const chartOptions = useMemo(() => ({\\n    responsive: true,\\n    maintainAspectRatio: false,\\n    plugins: {\\n      legend: { position: \\'top\\' },\\n      title: { display: false },\\n    },\\n    scales: {\\n      x: { display: true },\\n      y: { display: true, ticks: { maxTicksLimit: 6 } }\\n    }\\n  }), []);'; const newOptions = '  const chartOptions = useMemo(() => ({\\n    responsive: true,\\n    maintainAspectRatio: false,\\n    onHover: (event, activeElements) => {\\n      if (activeElements && activeElements.length > 0) {\\n        const idx = activeElements[0].index;\\n        const source = (sampledRows && sampledRows.length > 0 && useSampling) ? sampledRows : csvData;\\n        if (source && source[idx]) {\\n          const row = source[idx];\\n          const lat = row[\\'Latitudine\\'] || row[\\'Lat\\'] || row[\\'lat\\'];\\n          const lon = row[\\'Longitudine\\'] || row[\\'Lon\\'] || row[\\'lon\\'];\\n          if (lat && lon) {\\n            setHoveredCoords({ lat, lon });\\n          }\\n        }\\n      }\\n    },\\n    plugins: {\\n      legend: { position: \\'top\\' },\\n      title: { display: false },\\n    },\\n    scales: {\\n      x: { display: true },\\n      y: { display: true, ticks: { maxTicksLimit: 6 } }\\n    }\\n  }), [csvData, sampledRows, useSampling]);'; code = code.replace(oldOptions, newOptions); fs.writeFileSync('frontend_webbone/src/pages/DataVizualizer.jsx', code);
