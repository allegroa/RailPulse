const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function resetAll() { const users = await prisma.user.findMany(); const newHash = await bcrypt.hash('StrongP@ssw0rd', 10); let count = 0; for (let u of users) { await prisma.user.update({ where: { id: u.id }, data: { password: newHash } }); count++; } console.log('? Resettate le password per ' + count + ' utenti.'); await prisma.$disconnect(); } resetAll().catch(console.error);
