const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function fix() { const users = await prisma.user.findMany(); let count = 0; for (let u of users) { if (!u.password.startsWith('$') && !u.password.startsWith('$')) { console.log('Hashing password for', u.email); const h = await bcrypt.hash(u.password, 10); await prisma.user.update({ where: { id: u.id }, data: { password: h } }); count++; } } console.log('Updated ' + count + ' users'); await prisma.$disconnect(); } fix().catch(console.error);
